{
  "name": "lark-v2",
  "description": "Feishu/Lark channel plugin for Claude Code (v2 fork architecture) — receive messages via WebSocket, fork per-message headless claude -p subprocess, reply with text/images/files, manage cross-session memory. ⚠️ Disable v1.x `lark` plugin first; both share ~/.claude/channels/lark/ data.",
  "version": "1.3.2",
  "author": {
    "name": "IS908"
  },
  "repository": "https://github.com/IS908/claude-lark-plugin",
  "homepage": "https://github.com/IS908/claude-lark-plugin",
  "license": "Apache-2.0",
  "keywords": ["feishu", "lark", "im", "bot", "chat", "lite-memory"],
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/enforce-lark-reply.mjs",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
